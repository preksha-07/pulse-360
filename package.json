{
  "name": "pulse360",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspace=client && cp -r client/dist dist",
    "test": "npm run test --workspaces"
  }
}
